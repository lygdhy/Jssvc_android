<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_pressed="false">
        <shape>

            <!-- 填充 -->
            <solid android:color="#35d2ce" />

            <!-- 颜色渐变：渐变方向、开始颜色、结束颜色 -->
            <gradient android:angle="90" android:endColor="#35d2ce" android:startColor="#35d2ce" android:type="linear" />

            <!-- 按钮边缘：边缘宽、边缘颜色 -->
            <stroke android:width="0dp" android:color="#35d2ce" />

            <!-- 按钮四个圆角：半径 -->
            <corners android:radius="2dp" />

            <!-- 按钮文字和边缘距离(内边距) -->
            <padding android:bottom="0dp" android:left="6dp" android:right="6dp" android:top="0dp" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape>

            <!-- 填充 -->
            <solid android:color="#26c9c5" />

            <!-- 颜色渐变：渐变方向、开始颜色、结束颜色 -->
            <gradient android:angle="90" android:endColor="#26c9c5" android:startColor="#26c9c5" android:type="linear" />

            <!-- 按钮边缘 -->
            <stroke android:width="0dp" android:color="#26c9c5" />

            <!-- 按钮四个圆角：半径 -->
            <corners android:radius="2dp" />

            <!-- 按钮文字和边缘距离(内边距) -->
            <padding android:bottom="0dp" android:left="6dp" android:right="6dp" android:top="0dp" />
        </shape>
    </item>

</selector>