<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

  <item android:state_pressed="false">
    <shape>

      <!-- 填充 -->
      <solid android:color="@color/color_primary"/>

      <!-- 颜色渐变：渐变方向、开始颜色、结束颜色 -->
      <gradient android:angle="90" android:endColor="@color/color_primary" android:startColor="@color/color_primary" android:type="linear"/>

      <!-- 按钮边缘：边缘宽、边缘颜色 -->
      <stroke android:color="@color/color_primary" android:width="0dp"/>

      <!-- 按钮四个圆角：半径 -->
      <corners android:radius="0dp"/>

      <!-- 按钮文字和边缘距离(内边距) -->
      <padding android:bottom="0dp" android:left="0dp" android:right="0dp" android:top="0dp"/>
    </shape>
  </item>
  <item android:state_pressed="true">
    <shape>

      <!-- 填充 -->
      <solid android:color="@color/color_primary_pressed"/>

      <!-- 颜色渐变：渐变方向、开始颜色、结束颜色 -->
      <gradient android:angle="90" android:endColor="@color/color_primary_pressed" android:startColor="@color/color_primary_pressed" android:type="linear"/>

      <!-- 按钮边缘 -->
      <stroke android:color="@color/color_primary_pressed" android:width="0dp"/>

      <!-- 按钮四个圆角：半径 -->
      <corners android:radius="0dp"/>

      <!-- 按钮文字和边缘距离(内边距) -->
      <padding android:bottom="0dp" android:left="0dp" android:right="0dp" android:top="0dp"/>
    </shape>
  </item>

</selector>